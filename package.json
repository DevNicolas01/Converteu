{
  "name": "converteu-root",
  "private": true,
  "description": "Ferramentas de dev do projeto (testes de regras do Firestore). O app em si continua sendo estático (index.html/app.js/style.css).",
  "scripts": {
    "test:rules": "firebase emulators:exec --project=converteu-dec78 \"node firestore-rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.13.0"
  }
}
